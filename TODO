* introduce a new type (called Key or BtrfsKey?) combining (objectId, iType, offset) and define Bounded and Enum instances for it
* provide a high level function that implements functionality similar to btrfs subvolume find-new
* use BTRFS_IOC_TREE_SEARCH_V2 (introduced in 3.16)
* support BTRFS_IOC_LOGICAL_INO_V2 (introduced in 4.15)
